import { Home, User } from '../../scenes';

const tabNavigationData = [
    {
        name: 'Home',
        component: Home,
        label: 'home'
    },
    {
        name: 'User',
        component: User,
        label: 'user'
    }
];

export default tabNavigationData;